import { StdirectiveDirective } from './stdirective.directive';

describe('StdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
